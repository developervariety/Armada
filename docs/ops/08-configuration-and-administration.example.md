---
topic: "Configuration And Administration"
summary: "Administrator surfaces for this deployment: migrations, runners, credentials, and the memory folder."
read_when: "Changing settings, routing, migrations, or the typed-decision configuration."
applies_to: orchestrator
tier: leaf
---
# Configuration And Administration

> Template. Copy to `08-configuration-and-administration.md` in this folder and fill it in for your
> deployment. The filled copy is operator-local and is not committed.

Cover at least:

- Schema migration ledger and how to read the live version.
- Per-captain provider credentials: where each is configured (names, never values).
- The durable memory folder mount, if any.
- Typed decisions: see `docs/TYPED_DECISIONS.md`. Model routing: see `docs/USAGE_ROUTING.md`.
