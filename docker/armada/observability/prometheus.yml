global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: armada
    metrics_path: /metrics
    static_configs:
      - targets:
          - armada-server:9464
        labels:
          service: armada
